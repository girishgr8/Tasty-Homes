class User {
  String name, email, bio, phone, photo;
  int saved;
  var joinedDate;
  User({
    this.name,
    this.email,
    this.bio,
    this.joinedDate,
    this.photo,
    this.phone,
    this.saved,
  });
}
